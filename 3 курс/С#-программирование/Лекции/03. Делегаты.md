```cs
public delegate void DrawDelegate1();
public delegate void DrawDelegate2(int size);

public class Pen
{
    public int Size { get; set; }

    public void Draw(DrawDelegate1 how1, DrawDelegate2 how2)
    {
        how1();
        how2(Size);
    }
}
```
  
```cs
// public delegate void LogDelegate(string text, params string[] args);

public class L {
	public delegate void LogDelegate(string text);
	private void Log(LogDelegate logDelegate, string text) {
		logDelegate(text);
		log(Console.WriteLine, text);
	}
	
	public void LogConsole(string text) {
		Console.WriteLine(text);
	}
	
	public void LogDebug(string text) {
		Log(Debug.WriteLine, text);
	}
}
```

